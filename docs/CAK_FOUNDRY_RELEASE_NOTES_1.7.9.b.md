# Aurora CAK Foundry v1.7.9.b

## CLI baking

- Added `npm run bake:v93 -- --source <folder> --output <file>` for headless WWE 2K25 FDIR 9.3 baking.
- The command uses the same Bakery backend and byte-for-byte verification path as the desktop application.
- Optional `--tool` and `--oodle` arguments support local backend/toolchain locations.
